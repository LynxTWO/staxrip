# StaxRip Community Planning Unknowns

Version: 0.1 Draft. Date: 2026-08-15.

| ID | Unknown | Evidence and confidence | Risk | Owner | Next check | Blocks | Status |
|---|---|---|---|---|---|---|---|
| U-001 | Whether the public name `StaxRip Community` needs maintainer or trademark permission | unknown: the working name is selected, but no reviewed naming permission or trademark judgment is recorded. | A public release may need different branding even when the MIT license permits code reuse. | Human maintainer with legal judgment | Review upstream naming guidance and obtain human legal judgment before public binary distribution. | Public branding and release, not source planning | Open |
| U-002 | Safe upper bounds for source count, profiles, templates, jobs, and media size | unknown: no boundary benchmark or supported-limit contract is recorded. | Unfounded limits or scale claims could reject valid workflows or hide performance failures. | Maintainer for the feature that introduces a limit | Measure existing behavior with synthetic boundary fixtures before adding caps. | Future scale claims | Open |
| U-003 | A-002 and Q-001: whether existing in-memory state can supply the initial readiness catalog, which checks belong in it, and whether they support an honest overall status | verified: source-opening ownership exists in `Source/Forms/MainForm.vb`, D-032 targets five to eight pure checks with one correctable blocker, and existing encode entry has additional gates at `Source/Forms/MainForm.vb:4187,4195-4198`. unknown: which post-success facts satisfy ownership, purity, lifecycle, privacy, user value, and D-034 `Ready` authority. | The slice could duplicate legacy decisions, read side-effecting state, create unstable ids, or report `Ready` while an authoritative condition is unchecked. | `SLICE-001` mapping-spike owner | Trace candidate facts and existing encode gates to their owners and side effects, reject facts that require execution or mutation, prove one existing-control correction, document every overall status and unchecked boundary, and obtain catalog approval before model implementation. | `SLICE-001` model and tests | Open |
| U-004 | A-004 and Q-003: whether the selected synthetic fixtures represent initial compatibility and timing risk, and what protocol establishes the baseline | verified: numeric targets exist in ADD section 11. unknown: no source-bound fixture and timing artifact establishes the current baseline. | Performance claims could be unrepeatable or hide regressions in existing source opening. | `SLICE-001` verification owner | Approve synthetic fixtures, stage boundaries, warmup, repetition count, machine facts, and aggregate output, then record the pre-change baseline. | `SLICE-001` performance-dependent implementation and completion | Open |
| U-005 | State retained or persisted after each unexpected source-opening failure route | verified: for a non-abort exception, `MainForm` calls the global handler, which attempts recovery and diagnostics and then terminates the process. unknown: the retained project, settings, log, and recovery-file state across injected failure points. | A recovery change could destroy or misrepresent user-owned project state. | Future recovery-slice owner | Use bounded failure injection in a separate recovery slice before changing source-opening recovery. | Recovery improvements, not read-only readiness | Open |
| U-006 | Reproducible portable assembly, bundled-tool provenance, downgrade, and publication contract | verified: local x64 source builds exist. unknown: no approved artifact assembly and rollback record covers a public community binary. | A release could contain unverified tools, machine-specific state, or unsafe downgrade behavior. | Release-slice maintainer | Map and approve the release boundary in its own slice without running protected scripts during discovery. | Public binary distribution | Open |
| U-007 | The canonical steering file was not tracked on the fork baseline | verified: closed by root `AGENTS.md` in commit `5abceb83`, including the corrected x64-only graph and approval gates. | Closed. Without the file, contributors and agents could miss user-data and compatibility safeguards. | Repository maintainer | Re-audit when repository profile or approval boundaries change. | None | Closed |
| U-008 | Deterministic test-harness ownership was not selected | verified: closed by D-024 after a source scan found no existing automated test framework and the user approved a standalone source-linked x64 VB harness outside the solution. | Closed. An unreviewed test project could have changed dependencies or protected solution mappings. | `SLICE-001` verification owner | Revisit only when multiple suites justify common discovery, coverage, or CI. | None | Closed |
| U-009 | Whether legacy ClickOnce and bootstrapper publishing metadata is used, and what .NET Framework 4.8 prerequisite contract should replace it | verified: `Source/StaxRip.vbproj` retains `BootstrapperEnabled` and old publish fields, and x86 .NET 2.0 and 3.0 prerequisite entries were removed in `656c9818`. unknown: runtime use and maintainer intent. | Broad cleanup could silently change publish or distribution behavior. | Release-slice maintainer | Map publish-target behavior and history without running release scripts, then approve any cleanup in the release-boundary slice. | Public publishing and release, not normal x64 source builds | Open |
| U-010 | A-003 and Q-002: whether an Assistant-adjacent surface can fit, and its exact post-success ownership and placement | verified: the Assistant sits in the main window and has side-effecting script and frame-server behavior; existing source-loaded events fire inside the source-opening `Try`. inferred: an adjacent separately owned surface can fit. unknown: a safe event or call site after the transaction, runtime focus, scaling, and UI Automation behavior. | A poorly placed hook could enter the source-opening exception transaction, duplicate Assistant logic, or make the workflow less accessible. | `SLICE-001` seam and UI ownership-spike owner | Map transaction completion and control ownership, prove the adapter is not invoked on abort or failure, build a bounded prototype, run keyboard and accessibility inspection, and obtain user approval before production form edits. | `SLICE-001` adapter and GUI implementation | Open |
| U-011 | A-001: whether users benefit from one consolidated readiness summary after source opening | inferred: the selected workflow direction supports the idea, but no interactive prototype or user walkthrough has tested the value. | The slice could be technically correct yet fail to make the workflow clearer enough to justify continuing. | Human approver for `SLICE-001` | Review the first interactive prototype through the complete walkthrough. Collect broader public feedback only after an approved distribution path exists. | `SLICE-001` value validation and Done, not model implementation | Open |
| U-012 | Which state changes invalidate readiness after source opening | verified: encoder, muxer, target, script-engine, and requirement state can change after source opening; D-031 selects explicit refresh and mandatory invalidation. unknown: the safe invalidation trigger for every approved field, new-open start, failed later open, and project replacement. | A stale `Ready` result could remain visible for a different or changed effective project. | `SLICE-001` lifecycle-spike owner | Trace every approved snapshot field to its mutation owners, define project-transition invalidation, exclude fields without a safe trigger, and force stale-result scenarios before adapter implementation. | `SLICE-001` adapter and GUI implementation | Open |
| U-013 | Complete interactive, job, batch, recovery, and encoding call-path ownership for source opening | verified: `Source/General/GlobalClass.vb:561` calls `OpenVideoSourceFiles` with `isEncoding := True` during `ProcessJob`, while the readiness feature is interactive and excludes job behavior. unknown: every shared overload call site, selected-hook reachability, the pre-job owner that clears or hides prior transient state without creating a new result or presentation, and a safe isolated job-path evidence protocol. | Readiness or its verification could add latency, GUI work, nonfatal failure handling, tool execution, or output effects to job processing or user-owned jobs. | `SLICE-001` activation-spike owner | Map every source-opening caller and branch, source-trace the selected hook out of non-interactive paths, identify the synchronous pre-job clear or hide owner, and approve a synthetic isolated job-path protocol with a stopping point before processing, tool launch, or output work. If no safe protocol exists without changing job behavior, stop for a new decision. M2 and L3 later prove production activation counts and outcomes. | `SLICE-001` adapter implementation and L3 job-path evidence | Open |
| U-014 | Deterministic activation seam for readiness-owned snapshot and evaluation failures | verified: planned snapshot mapping is separate from evaluation, and current UI exceptions route through `Source/Forms/MainForm.vb:993` and `Source/General/GlobalClass.vb:1650-1655` to the mapped fatal path at line 1591. unknown: a bounded production dependency seam that can force both branches without a global or user-triggered switch. | Mapper failure could terminate the process or remain untested while evaluator failure appears covered. | `SLICE-001` failure-spike owner | Select a coordinator dependency or fault-injection seam, demonstrate both fault branches in an ignored disposable probe, and reject any option that needs a package, solution mapping, environment trigger, or broader global hook. M2 and L4 later prove production activation and outcomes. | `SLICE-001` adapter implementation and L4 evidence | Open |

Review this table at every document audit. Close an item only with repository evidence or an explicit human decision. Open items inside an active slice must have a closing milestone before dependent work.

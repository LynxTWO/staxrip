# StaxRip Community Planning Unknowns

Version: 0.1 Draft. Date: 2026-08-15.

| ID | Unknown | Evidence and confidence | Risk | Owner | Next check | Blocks | Status |
|---|---|---|---|---|---|---|---|
| U-001 | Whether the public name `StaxRip Community` needs maintainer or trademark permission | unknown: the working name is selected, but no reviewed naming permission or trademark judgment is recorded. | A public release may need different branding even when the MIT license permits code reuse. | Human maintainer with legal judgment | Review upstream naming guidance and obtain human legal judgment before public binary distribution. | Public branding and release, not source planning | Open |
| U-002 | Safe upper bounds for source count, profiles, templates, jobs, and media size | unknown: no boundary benchmark or supported-limit contract is recorded. | Unfounded limits or scale claims could reject valid workflows or hide performance failures. | Maintainer for the feature that introduces a limit | Measure existing behavior with synthetic boundary fixtures before adding caps. | Future scale claims | Open |
| U-003 | A-002 and Q-001: which pure checks form the first Source project checks catalog and its bounded overall status | verified: closed by the three-check catalog, bounded status semantics, rejected-candidate record, repository-owned mutation map, 12,199-assertion source-linked harness, and 1,999-assertion product-assembly mapper probe in `../Verification/SLICE-001/M2-INTEGRATION.md`. D-037 replaces `Ready` with `Selected checks passed` and no encode authorization. Four paired candidate routes publish `Available` with exactly three checks, and malformed-later-open ends Hidden. | Closed for catalog, deterministic production mapping, successful route publication, and one stale-state challenge. Complete MainForm and every mapped visible transition remain L4 evidence. | `SLICE-001` catalog owner | Reopen if a check, input, status, message argument, or authority claim changes. | None; remaining GUI gates do not reopen the catalog decision | Closed |
| U-004 | A-004 and Q-003: whether the selected synthetic fixtures represent initial compatibility and timing risk, and what protocol establishes the baseline | verified: five fixture shapes, exact ignored bytes, complete recipe, tool and donor identities, accepted warmup and alternating paired protocol, and privacy-safe aggregate are recorded in `../Verification/SLICE-001/M0-RUNTIME-PLAN.md`. Four baseline/candidate route blocks each discard three warmups and pass 20 measured pairs with matching template, workflow-contract, and media-fact identities. Candidate median deltas are +23.128 ms AVS, +17.558 ms VPY, +38.525 ms MP4, and +0.730 ms MKV, all within their 100.000 or 111.353 ms allowance. The slowest-fixture 50-refresh block has p95 1.311 ms and handles 625 to 624. The malformed-later-open challenge ends Hidden. Independent postflight passes 15 checks with zero task processes and mappings. | Closed for the approved compatibility, timing, stale-state, and handle protocol. Exact `Application.Run`, operating-system accessibility, physical input, and human value remain separate completion evidence. | `SLICE-001` verification owner | Reopen if a relevant product, fixture, template, host, driver, protocol, or protected-content identity changes. | None for performance acceptance; remaining GUI and human gates do not reopen this protocol result | Closed |
| U-005 | State retained or persisted after each unexpected source-opening failure route | verified: for a non-abort exception, `MainForm` calls the global handler, which attempts recovery and diagnostics and then terminates the process. unknown: the retained project, settings, log, and recovery-file state across injected failure points. | A recovery change could destroy or misrepresent user-owned project state. | Future recovery-slice owner | Use bounded failure injection in a separate recovery slice before changing source-opening recovery. | Recovery improvements, not read-only project checks | Open |
| U-006 | Reproducible portable assembly, bundled-tool provenance, downgrade, and publication contract | verified: local x64 source builds exist. unknown: no approved artifact assembly and rollback record covers a public community binary. | A release could contain unverified tools, machine-specific state, or unsafe downgrade behavior. | Release-slice maintainer | Map and approve the release boundary in its own slice without running protected scripts during discovery. | Public binary distribution | Open |
| U-007 | The canonical steering file was not tracked on the fork baseline | verified: closed by root `AGENTS.md` in commit `5abceb83`, including the corrected x64-only graph and approval gates. | Closed. Without the file, contributors and agents could miss user-data and compatibility safeguards. | Repository maintainer | Re-audit when repository profile or approval boundaries change. | None | Closed |
| U-008 | Deterministic test-harness ownership was not selected | verified: closed by D-024 after a source scan found no existing automated test framework and the user approved a standalone source-linked x64 VB harness outside the solution. | Closed. An unreviewed test project could have changed dependencies or protected solution mappings. | `SLICE-001` verification owner | Revisit only when multiple suites justify common discovery, coverage, or CI. | None | Closed |
| U-009 | Whether legacy ClickOnce and bootstrapper publishing metadata is used, and what .NET Framework 4.8 prerequisite contract should replace it | verified: `Source/StaxRip.vbproj` retains `BootstrapperEnabled` and old publish fields, and x86 .NET 2.0 and 3.0 prerequisite entries were removed in `656c9818`. unknown: runtime use and maintainer intent. | Broad cleanup could silently change publish or distribution behavior. | Release-slice maintainer | Map publish-target behavior and history without running release scripts, then approve any cleanup in the release-boundary slice. | Public publishing and release, not normal x64 source builds | Open |
| U-010 | A-003 and Q-002: whether an Assistant-adjacent surface can fit, and its exact post-success ownership and placement | verified: the coordinator runs after the source `Try/Catch/Finally`; Assistant is side-effecting; D-037 approves the position opt-out. The strict ignored x64 model verifies the visible three-plus-one layout, exact copy, menu rebuild, modeled keyboard and focus route, accessibility metadata, and position opt-out. The production refinement keeps the summary attached, restores the Assistant's four-column span while checks are Hidden, and switches to three-plus-one for visible states. The production source shape passes 87 static assertions, and the production-control probe passes the expanded bundled-font matrix. | Closed for placement and source ownership. Complete MainForm behavior, physical DPI, high contrast, Narrator/UIA, and physical keyboard remain L4 evidence rather than an unresolved design decision. | `SLICE-001` UI owner | Reopen if production cannot preserve the refined placement, baseline hidden layout, or non-authorization copy. | None for design; production completion still needs L4 evidence | Closed |
| U-011 | A-001: whether users benefit from one consolidated project-check summary after source opening | inferred: the selected workflow direction, production controls, and deterministic presentation probes support the idea, but no complete interactive MainForm or user walkthrough has tested the value. | The slice could be technically correct yet fail to make the workflow clearer enough to justify continuing. | Human approver for `SLICE-001` | Review the production workflow through the complete walkthrough. Collect broader public feedback only after an approved distribution path exists. | `SLICE-001` value validation and Done, not model implementation | Open |
| U-012 | Which state changes invalidate project checks after source opening | verified: the repository-owned mutation enumeration and exact owners are in `../Verification/SLICE-001/M0-CATALOG.md`; M2 implements balanced clearing and invalidating scopes, generation publication guards, token revocation, and stale-interleaving regressions. The final catalog excludes script/filter, package, disk, job, output, and polymorphic muxer-compatibility state. | Closed for lifecycle ownership and deterministic integration. A visible stale result would still be a defect, so L4 must compare every loaded production presentation transition. | `SLICE-001` lifecycle owner | Reopen if a retained input gains a new mutation route or an invalidation hook cannot be preserved. | None for ownership; production completion still needs visible stale-state evidence | Closed |
| U-013 | Complete interactive, job, batch, recovery, and encoding call-path ownership for source opening | verified: repository callers, hidden event-command reentrancy, activation-policy inputs, success-only seam, both synchronous clear owners, 8,192 activation vectors, and 87 focused production-source assertions are recorded in the M0 and M2 evidence. verified: a successful real `ProcessJob` check cannot stop before source-opening tool work. | Closed. Actual successful job execution remains a later isolated tool-boundary approval and is not claimed by this slice. | `SLICE-001` lifecycle owner | Retain the caller map, vectors, and source assertions; run only the approved interactive L3 branches. | None; revised S-018 deterministic evidence passes | Closed |
| U-014 | Deterministic activation seam for project-check-owned snapshot and evaluation failures | verified: constructor-injected mapper and evaluator delegates force both branches in the source-linked harness and the isolated product-assembly probe documented at `../Verification/SLICE-001/M2-INTEGRATION.md`. Both converge to the same payload-free `Unavailable` state while 175 shallow project fields remain unchanged. | Closed for seam design and deterministic production composition. Loaded-project GUI activation remains L4 outcome evidence. A permanent global, environment, or user-triggered fault hook would reopen the risk. | `SLICE-001` coordinator owner | Retain disposable composition-root substitutions and activate both loaded GUI branches in L4. | None for the seam; production completion still requires L4 | Closed |

Review this table at every document audit. Close an item only with repository evidence or an explicit human decision. Open items inside an active slice must have a closing milestone before dependent work.

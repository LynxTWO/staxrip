# StaxRip Community Coverage Ledger

Version: 0.4. Date: 2026-08-16. Repository base: `940eaba1`.

This ledger records risk-ranked review coverage. It does not convert partial inspection, a clean build, or one workflow into a whole-repository claim. Confidence uses only `verified`, `inferred`, or `unknown`.

| Area | Risk | Confidence | Evidence covered | Boundary not covered | Next check and owner |
|---|---|---|---|---|---|
| x64 solution and project graph | High | verified | Project XML parsed; configuration declarations are x64-only; current direct managed Debug and Release x64 rebuilds and current solution Debug and Release x64 rebuilds pass. The historical solution Release mapping builds managed StaxRip as Debug; the separate direct Release project build passes and no mapping changed | Publish targets and remaining ClickOnce metadata | U-009 release-slice owner maps publish behavior before cleanup |
| Source-opening transaction | High | verified | Entry routing, state changes, tools, events, catches, recovery, every repository caller, worker-event reentrancy, extra-source child launch, and project-replacement assignments are mapped in `../Verification/SLICE-001/M0-SYSTEM-MAP.md` and `../Verification/SLICE-001/M0-ACTIVATION-LIFECYCLE.md`; the production completion seam and balanced scopes pass the 87-assertion source contract in `../Verification/SLICE-001/M2-INTEGRATION.md`; the reviewed v6 matrix in `../Verification/SLICE-001/M0-RUNTIME-PLAN.md` completes 186 task-owned host runs across four source routes and one malformed-later-open challenge | Exact `Application.Run`, abort, recovery, overlap, and nested runtime activation remain untested | L3 finishes the remaining approved interactive branches without claiming job equivalence |
| Shared source-opening activation | High | verified | The source-linked policy covers transaction success, source and mutation depth, encoding, UI-thread entry, entry and completion job/batch state, startup `-NoFocus` scope, and same-project identity across 8,192 vectors; focused production-source assertions verify both clear owners; every measured candidate source open reaches `Available` with exactly three checks | No safe successful real-job protocol can stop before source-opening tool work; rejected runtime contexts and exact `Application.Run` remain untested | D-037 excludes job equivalence; L3 records remaining approved clear and rejection outcomes |
| Source project checks catalog and snapshot mapping | High | verified | `../Verification/SLICE-001/M0-CATALOG.md` approves three pure checks; the product-assembly probe in `../Verification/SLICE-001/M2-INTEGRATION.md` verifies every bounded mapper state, fault convergence, and 175 unchanged shallow project fields with 1,999 assertions; four paired runtime routes preserve exact workflow-contract and media-fact parity | The human value walkthrough remains untested; external reflection mutation stays unknown | M4 human review owns value; reopen mapping evidence if a retained identity changes |
| Project-check invalidation and refresh | High | verified | Repository-owned mutation sites are mapped in `../Verification/SLICE-001/M0-CATALOG.md`; production hooks, separate mutation counters, generation publication guards, and 1,280 refresh-policy vectors pass the M2 harness and source contract; the runtime malformed-later-open challenge ends Hidden and 50 measured refreshes pass | Complete visible outcomes for every mapped mutation owner and hostile external in-process reflection stay unknown | L4 activates every remaining mapped presentation transition |
| Current fatal UI exception route for project-check integration | High | verified | The production coordinator exists and the product-assembly probe forces mapper and evaluator failures to the same payload-free `Unavailable` state without changing 175 shallow fields; the narrow catch excludes publication and rendering | A complete loaded-project GUI runtime branch has not activated either fault; broader UI exceptions retain the existing fatal route | L4 activates both faults in the isolated production presentation |
| Unexpected source-opening failure | High | verified | `Source/Forms/MainForm.vb:2925-2926` calls the global handler; `Source/General/GlobalClass.vb:1570-1597` attempts recovery and diagnostics and terminates for a non-abort exception | Retained project, settings, recovery-file, and log state at injected failure points | U-005 recovery-slice owner performs separate failure injection before recovery changes |
| Project, settings, template, profile, and job persistence | High | unknown | Planning guardrails name these as compatibility surfaces and exclude changes from `SLICE-001` | Format readers, writers, migrations, repair, and downgrade behavior | Map only when a separately approved persistence slice touches them |
| External commands and process control | High | unknown | Planning maps `Proc`, `ProcController`, and `Package` as protected owners; `SLICE-001` forbids new execution or selection | Escaping, cancellation, retry, inherited environment, and outcome behavior across tools | A later command or process slice owns focused mapping and deterministic probes |
| Native FrameServer boundary | High | verified | `Source/FrameServer/FrameServer.vcxproj` is x64-only and built in both solution configurations | ABI, registration, script-engine ownership, inter-process lifetime, and failure injection | Separate native-boundary approval and harness before behavior changes |
| Existing WinForms layout and accessibility precedents | High | verified | Strict design evidence selects the 177-logical-pixel visible three-plus-one layout after testing 141 bundled selectable Default font families across ten combined scale factors; the production source shape restores the Assistant's four-column span while checks are Hidden and uses three columns only for visible states. It also verifies exact copy, dynamic result names, stale-details closure, high-contrast button borders, menu rebuild, accessibility metadata, and position opt-out | Complete MainForm UI Automation, Narrator, physical keyboard, actual high contrast, and physical 100 through 200 percent DPI behavior remain untested | L4 records production and human observations |
| Existing logging exposure and project-check privacy decision | High | verified | Selected legacy source-opening paths can write full paths, media summaries, generated scripts, and arbitrary errors; the implemented project-check files pass a bounded scan for no new log, raw exception, process, filesystem, network, or clipboard boundary | Whole-repository runtime flow of debug, trace, tool, and support content is not covered | L4 privacy sentinels and bounded output review cover the new presentation; broader logging audit remains separate |
| Test-harness inventory and ownership decision | Medium | verified | The package-free standalone .NET Framework 4.8 x64 harness source-links six pure production files outside the solution and passes Debug and Release with 12,199 assertions, 8,192 activation vectors, and 1,280 refresh vectors; higher-level product-assembly, production-control, and reviewed runtime evidence is indexed under `../Verification/SLICE-001/README.md` | Complete MainForm and operating-system presentation still require human and physical checks | L4 finishes presentation evidence without adding a solution mapping |
| Source-opening timing and handle baseline | Medium | verified | The reviewed v6 Release x64 matrix in `../Verification/SLICE-001/M0-RUNTIME-PLAN.md` binds products, driver, host, templates, fixtures, contracts, environment, and aggregate summary. Four routes each pass three baseline and candidate warmups plus 20 alternating measured pairs. Candidate median deltas are +23.128, +17.558, +38.525, and +0.730 ms within allowance. Refresh p95 is 1.311 ms; handles change 625 to 624; malformed-later-open ends Hidden; independent 15-check postflight finds zero task processes and mappings | Exact `Application.Run` and complete OS and human presentation are outside the performance protocol | U-004 is Closed; rerun when a relevant bound identity or protocol changes |
| Build, packaging, and public release | High | unknown | Current Windows x64 local source builds and source-only deployment posture are verified; protected release scripts were not run | Reproducible portable assembly, tool provenance, signing, publication, and downgrade | U-006 and U-009 remain release-slice blockers |
| Planning and repository steering | Medium | verified | Root `AGENTS.md` is tracked; D-037 is Confirmed and supersedes D-030 and D-034; M0 through M3 deterministic evidence and the M4 paired runtime result are indexed under `../Verification/SLICE-001/README.md`; Q-003 and U-004 are Closed | Complete production accessibility, remaining branch evidence, and human value remain open | Close U-011 and the remaining SLICE-001 brief gates before that slice is complete |
| Current Windows portability blockers | High | verified | `../Architecture/Portability-System-Map.md` maps the .NET Framework 4.8 WinForms entry, global project ownership, BinaryFormatter persistence, Windows process and registry paths, and COM frame server with source citations | Detailed behavior of every tool adapter, cleanup owner, and persisted subtype | P-001 through P-005 drive separate risk-ranked slices |
| Implemented cross-platform contracts and core source | High | verified | `CrossPlatform/src/StaxRip.Contracts`, `StaxRip.Core`, and `StaxRip.Platform` implement the bounded capability contracts, one-way source graph, fixed catalog, host-fact interface, and host-fact adapter; Debug and Release each pass 23 cases and 309 assertions | No media workflow, external-tool adapter, or legacy project boundary is extracted | S-PORT-02 maps one read-only media-inspection contract before adding input authority |
| Implemented loopback server and web-shell source | High | verified | The five-route host has latest pre-freeze passes for the 4,532-check Release HTTP matrix and 758-check installed Edge boundary, plus recorded WSL loopback and sandbox, session separation, privacy sentinel, and clean-shutdown checks. HTTP and browser ownership starts before readiness and binds exact PID, start time, executable, and arguments; live pipes are capped and success requires reap, port release, cleanup, and empty task roots. The browser additionally correlates `Browser.getVersion`, `SystemInfo.getProcessInfo` OS PIDs, and executable hash | Final committed-source correspondence is pending. No mutation, media, remote, shared-host, hostile same-user process, Linux GUI-browser behavior, or browser-process background traffic is covered | Re-run HTTP, browser, and final evidence from one committed checkpoint; reopen P-006 before authority expands |
| Linux runtime and tool environment | High | verified | The exact self-contained Linux x64 tree runs as non-root in local WSL without an installed .NET command; socket, HTTP, manifest, no-write, no-child, and shutdown checks pass. The same immutable tree also ran on the independent Ubuntu 24.04.4 peer over Tailscale SSH on 2026-08-17 and passed 27 checks with `runtime_writes=0`, but only while that host mitigation was temporarily relaxed and then restored; under the normal configuration the gate fails `sandbox-activation-network-not-private` | Separate-host behavior, tool versions, GPU encode, filesystem differences, and Linux GUI browser | Source a host whose unprivileged user units can obtain a private network namespace, or give the gate a documented reduced-isolation mode; R-S2-039 owns closure |
| macOS runtime, native UI, and packaging | High | unknown | Contract-level portability and source-level macOS host-fact normalization are present; P-008 and P-010 record the missing runner, tool, UI, signing, and notarization evidence. Work is intentionally deferred | All macOS runtime and distribution behavior | Begin only after Linux inspection and execution contracts stabilize |
| Cross-platform local build and Linux test publish | High | verified | Focused local restore, Debug and Release build, contract, self-contained publish, whole-tree identity, Windows HTTP, installed Edge, and WSL gates have passed at recorded checkpoints. The latest clean 398-check restore derives three pinned downloads from five reviewed project inputs, separates registry, archive, signature, and extracted identities, and binds each signed `.nupkg` plus the canonical complete disk inventory under schema v2. A nuspec byte and line-ending mutation failed closed before the ignored cache was quarantined intact and restored from the approved source | The latest independent review returned NO-GO. Final committed-source rerun, `port-evidence` correspondence, and re-review are pending. Independent Ubuntu, macOS, public artifact provenance, and current Windows application regression remain outside this result | Finish R-S2-026 through R-S2-032, close the four documentation assertions the final auditor stops on, repeat independent review, then resolve R-S2-039 without promoting local-test output to a release |
| Cross-platform verification evidence publication | High | verified | Every audited producer uses one atomic cross-OS `.evidence-writer.lock` receipt. Restore holds it across its whole mutable workflow and separates partial acquisition from validated ownership and invalidation. `Verify.ps1` retains canonical `.port-verify-running` across its child sequence. The final auditor's source rejects that marker plus case-drifted or nonempty task roots at primary and both closeouts. Two-direction Windows/WSL contention and focused marker, root, graph, binder, and redaction probes pass | Full final-auditor execution from the committed checkpoint and independent re-review remain pending. A page-target CDP trace does not observe every browser-process subsystem | Run all producers, the bounded Restore failure challenge, all three closeout snapshots, and final audit; R-S2-022 and R-S2-026 through R-S2-032 own closure |
| Public cross-platform release | High | unknown | SLICE-002 permits ignored local-test output only. Public distribution remains approval-gated | Reproducibility, provenance, signing, update, downgrade, branding, support, and publication | P-009 plus U-001, U-006, and U-009 require a later release slice |

Update this ledger when a touched path gains or loses evidence. A higher confidence in one row does not imply coverage of adjacent rows.
